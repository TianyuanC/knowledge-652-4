import { Head } from "mdx-deck";
export { future as theme } from "mdx-deck/themes";

<Head>
    <title>Knowledge Session (4) 🤯</title>
</Head>

# Knowledge Session (4) 🤯

---

## [Illustrations](https://illustrated.dev/)

---

## Web Components

-   Shadow DOM
-   [doc](https://developer.mozilla.org/en-US/docs/Web/Web_Components)
-   [source](https://www.dannymoerkerke.com/blog/web-components-will-replace-your-frontend-framework)
-   Applications
    -   [Material](https://dannymoerkerke.github.io/material-webcomponents)
    -   [UI5](https://sap.github.io/ui5-webcomponents/)

---

## Redux Dev Tool

![devTool](https://raw.githubusercontent.com/TianyuanC/knowledge-652-4/master/img/redux_devTool.png)

---

## Setup

-   [Doc](http://extension.remotedev.io/#13-use-redux-devtools-extension-package-from-npm)
-   [Tetris Demo](https://chvin.github.io/react-tetris/?lan=en)

---

import { CodeSurfer } from "mdx-deck-code-surfer";

<CodeSurfer
    title="Redux DevTools"
    code={require("!raw-loader!./code/redux_devtool.js")}
    lang="javascript"
    dark={false}
    steps={[
        { notes: "redux devtool setup" },
        {
            lines: [2],
            notes: "npm install --save-dev redux-devtools-extension"
        },
        { lines: [6], notes: "composer" },
        { lines: [7], notes: "enhancer" }
    ]}
/>

---

## Dev Tip:

`Console.log()`

[source](https://areknawo.com/getting-creative-with-the-console-api/)

---

![graphQL](https://raw.githubusercontent.com/TianyuanC/knowledge-652-4/master/img/graphql_1.png)

---

## Graphql

-   Why?
-   REST vs GraphQL
-   Demo

---

import Code from "mdx-code";

## ES6 Challenge

-   Promise: batching
-   Promise: sequential
-   Mixings

---

export default Code;

```javascript Promise: Batching
const delay = (t, name) => () =>
    new Promise(resolve => {
        setTimeout(() => {
            console.log(`done: task ${name}`);
            resolve();
        }, t);
    });
const task1 = delay(1000, "one");
const task2 = delay(500, "two");
const task3 = delay(600, "three");
const tasks = [task1, task2, task3];

const execute = () => {
    Promise.all(tasks.map(task => task()));
};

execute();
```

---

export default Code;

```javascript Promise: Sequential
const delay = (t, name) => () =>
    new Promise(resolve => {
        setTimeout(() => {
            console.log(`done: task ${name}`);
            resolve();
        }, t);
    });
const task1 = delay(1000, "one");
const task2 = delay(500, "two");
const task3 = delay(600, "three");
const tasks = [task1, task2, task3];

const execute = () => {
    tasks.reduce(
        (acc, currTask) => acc.then(() => currTask()),
        Promise.resolve()
    );
};

execute();
```

---

export default Code;

```javascript Promise: Mixing 🤯
const delay = (t, name) => () =>
    new Promise(resolve => {
        setTimeout(() => {
            console.log(`done: task ${name}`);
            resolve();
        }, t);
    });
const task1 = delay(1000, "one");
const task2 = delay(500, "two");
const task3 = delay(600, "three");
const tasks = [[task1, task2], task3];

const execute = () => {
    Promise.all(
        tasks.map(task =>
            Array.isArray(task)
                ? task.reduce(
                      (acc, currTask) => acc.then(() => currTask()),
                      Promise.resolve()
                  )
                : task()
        )
    );
};

execute();
```

---

<CodeSurfer
    title="Promise: Mixing"
    code={require("!raw-loader!./code/promise_mix.js")}
    lang="javascript"
    dark={false}
    steps={[
        { notes: "promise mixing" },
        {
            lines: [2],
            notes: "batching"
        },
        {
            lines: [3, 8],
            notes: "If it's task, execute, otherwise, apply sequential rules"
        },
        { range: [4, 7], notes: "sequential" }
    ]}
/>

---

## Video

<iframe
    width="840"
    height="473"
    src="https://www.youtube.com/embed/SSLrz5RsmnE"
    frameBorder="0"
    allow="accelerometer; autoplay; encrypted-media; gyroscope; picture-in-picture"
    allowFullScreen
/>

---

## Until Next Time 👋
